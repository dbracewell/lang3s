{
  "name": "lang3s-backend",
  "version": "1.0.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "scripts": {
    "check-deps": "uv run tach check-external",
    "add-dep": "uv add $DEP --package $PKG",
    "sync": "uv sync --all-packages",
    "uv": "uv run",
    "wipe-db": "uv run --directory packages/data/ --package lang3s-data python init_db.py",
    "bootstrap-db": "uv run --directory packages/data/ --package lang3s-data python bootstrap_db.py",
    "reset": "uv run --directory packages/data/ --package lang3s-data python init_db.py"
  }
}
